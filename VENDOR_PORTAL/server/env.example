# SAP Configuration
SAP_BASE_URL=http://172.17.19.24:8000
SAP_CLIENT=100
# Base64 encoded username:password (e.g., echo -n "user:pass" | base64)
SAP_BASIC=YWRtaW46YWRtaW4=

# Server Configuration
PORT=4000
CLIENT_ORIGIN=http://localhost:4200

# Caching Configuration
USE_CACHE=true
CACHE_TTL_MS=300000

# SAP Endpoint Configuration
# If true, use single ICF path for all RFCs (SOAPAction differentiates)
# If false, derive per-function paths (e.g., /sap/bc/srt/scs/sap/zrfc_<function>)
SAP_SINGLE_ENDPOINT=false
# If SAP_SINGLE_ENDPOINT=true, specify the single path here:
# SAP_SINGLE_ENDPOINT_PATH=/sap/bc/srt/scs/sap/zrfc_login_auth_829?sap-client=100

